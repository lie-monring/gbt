appId: com.gbt.app
productName: GBT
copyright: Copyright © 2026

directories:
  output: release

win:
  target:
    - target: portable
      arch: [x64]
  artifactName: ${productName}-${version}-portable.${ext}

files:
  - dist/**/*
  - dist-electron/**/*
  - package.json
